# Spark

## 组件

* SparkContext 向资源管理器注册并申请运行 Executor
* 资源管理器分配并启动 Executor，Executor 启动后向资源管理器发送心跳
* SparkContext 构建作业 DAG，将 DAG 分解为 Stage 发送给 TaskScheduler
* Executor 向 SparkContext 申请 Task，TaskScheduler 将 Task 发送给 Executor 运行

## 运行


## 配置

## 部署

### standalone

### on yarn

## 开发

``` scala
object HdfsWordCount {
  def main(args: Array[String]) {
    if (args.length < 1) {
      System.err.println("Usage: HdfsWordCount <directory>")
      System.exit(1)
    }

    StreamingExamples.setStreamingLogLevels()
    val sparkConf = new SparkConf().setAppName("HdfsWordCount")
    // Create the context
    val ssc = new StreamingContext(sparkConf, Seconds(2))

    // Create the FileInputDStream on the directory and use the
    // stream to count words in new files created
    val lines = ssc.textFileStream(args(0))
    val words = lines.flatMap(_.split(" "))
    val wordCounts = words.map(x => (x, 1)).reduceByKey(_ + _)
    wordCounts.print()
    ssc.start()
    ssc.awaitTermination()
  }
}
```

### RDD (Resilient Distributed Datasets)

表示一个数据集

#### 创建

从外部存储系统创建

``` scala
    val lines = ssc.textFileStream(args(0))
```

通过已有的 RDD 转换

``` scala
    val words = lines.flatMap(_.split(" "))
```

#### transformation 算子

* map(func)
* filter(func)
* flatMap(func)
* groupByKey([numTasks])
* reduceByKey(func, [numTasks])

#### action 算子

* collect()
* count()
* saveAsTextFile(path)


### DAG (Directed Acyclic Graph)

指 RDD 数据转换的过程，从 RDD 的创建开始，到 action 算子触发结束。

### Spark SQL

#### DataFrame / DataSet

``` scala
  type DataFrame = Dataset[Row]
```

#### DSL

``` scala
    val spark = SparkSession
      .builder()
      .appName("Spark SQL basic example")
      .config("spark.some.config.option", "some-value")
      .getOrCreate()

    // $example on:create_df$
    val df = spark.read.json("examples/src/main/resources/people.json")

    // Select only the "name" column
    df.select("name").show()
```

#### SQL

``` scala
    val spark = SparkSession
      .builder()
      .appName("Spark SQL basic example")
      .config("spark.some.config.option", "some-value")
      .getOrCreate()

    // $example on:create_df$
    val df = spark.read.json("examples/src/main/resources/people.json")

    // $example on:run_sql$
    // Register the DataFrame as a SQL temporary view
    df.createOrReplaceTempView("people")

    val sqlDF = spark.sql("SELECT * FROM people")
```

### Spark Streaming

#### Dstream (Discretized Stream)

一系列时间上连续的 RDD

#### Transformation 算子

* map(func)
* flatMap(func)
* filter(func)
* updateStateByKey
* window operations

#### Action/Output 算子

* print()
* foreachRDD(func)
* saveAsTextFiles(prefix, [suffix])

### Structured Streaming


